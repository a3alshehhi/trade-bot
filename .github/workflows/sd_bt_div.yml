name: SD Divergence Backtest (bullish MACD4C divergence gate)

on:
  workflow_dispatch:
    inputs:
      basket:
        description: "عدد رموز الكريبتو"
        default: '40'
      os_lookback:
        description: "نافذة الدايفرجنس"
        default: '100'
      max_ema_dist:
        description: "أقصى بُعد فوق المتوسط"
        default: '0.06'
      trend_ma:
        description: "فلتر الاتجاه"
        type: choice
        default: 'sma50'
        options:
          - live
          - sma50
          - sma100
          - sma200
          - ema50
          - ema100
          - ema200

permissions:
  contents: read

jobs:
  backtest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install deps
        run: pip install requests scikit-learn joblib
      - name: Backtest 15m (context 1h)
        env:
          SD_ENTRY_TF: 15m
          SD_HTF: 1h
          SD_BASKET: ${{ inputs.basket }}
          SD_OS_LOOKBACK: ${{ inputs.os_lookback }}
          SD_MAX_EMA_DIST: ${{ inputs.max_ema_dist }}
          SD_TREND_MA: ${{ inputs.trend_ma }}
        run: python sd_bt_div.py 2>&1 | tee -a sd_bt_div_log.txt
      - name: Backtest 1h (context 4h)
        env:
          SD_ENTRY_TF: 1h
          SD_HTF: 4h
          SD_BASKET: ${{ inputs.basket }}
          SD_OS_LOOKBACK: ${{ inputs.os_lookback }}
          SD_MAX_EMA_DIST: ${{ inputs.max_ema_dist }}
          SD_TREND_MA: ${{ inputs.trend_ma }}
        run: python sd_bt_div.py 2>&1 | tee -a sd_bt_div_log.txt
      - name: Upload log artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sd-bt-div-log
          path: sd_bt_div_log.txt
          if-no-files-found: ignore
