name: trendwave

# استراتيجية trendwave المستقلة (الإعداد الرابح):
# دخول سوقي مباشر بعد موجة RSI(20→80) + DCA فيبو، فلتر اتجاه من فريم أعلى،
# وقف متحرك 0.5×ATR مؤجّل +1R، بلا خروج دايفرجنس. تعمل على كل الفريمات.
# يُشغَّل يدوياً من تبويب Actions ← Run workflow. يطبع التقرير، يرسله لتيليجرام،
# ويرفع ملف CSV كأثر (artifact).
on:
  workflow_dispatch:
    inputs:
      timeframe:
        description: "الإطار الزمني"
        type: choice
        options: ["1d", "4h", "1h", "15m"]
        default: "4h"
      assets:
        description: "الأصول"
        type: choice
        options: [crypto, stocks, all]
        default: crypto
      bt_bars:
        description: "عدد الشموع للاختبار"
        default: "8000"
      bt_hold:
        description: "أقصى شموع لإمساك الصفقة"
        default: "60"
      cost:
        description: "تكلفة الصفقة ذهاب/إياب (0.002 = 0.2%)"
        default: "0.002"
      hl_gate:
        description: "بوابة موجة 2: قاع أعلى + عمق فيبو 0.382–0.786 (TW_HL_GATE)"
        type: choice
        options: ["0", "1"]
        default: "0"
      w1_targets:
        description: "أهداف من إسقاط الموجة 1 (TW_W1_TARGETS)"
        type: choice
        options: ["0", "1"]
        default: "0"
      extra_gates:
        description: "بوابات إضافية مثل: TW_SWEEP=1 TW_VOLZ=1.5 TW_GOLDEN=1 TW_RSI_MIN=60"
        default: ""

permissions:
  contents: read

jobs:
  trendwave:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install deps
        run: pip install -r requirements.txt

      - name: Run trendwave backtest
        env:
          TW_HL_GATE: ${{ inputs.hl_gate }}
          TW_W1_TARGETS: ${{ inputs.w1_targets }}
        run: |
          set -o pipefail
          EXTRA="${{ inputs.extra_gates }}"
          if [ -n "$EXTRA" ]; then export $EXTRA; fi
          python trading_bot.py --mode backtest \
            --watchlist watchlist.txt \
            --assets "${{ inputs.assets }}" \
            --timeframe "${{ inputs.timeframe }}" \
            --trendwave \
            --bt-bars "${{ inputs.bt_bars }}" \
            --bt-hold "${{ inputs.bt_hold }}" \
            --min-score 0 \
            --cost "${{ inputs.cost }}" \
            --output-dir . 2>&1 | tee trendwave_log.txt

      - name: Send report to Telegram
        if: always()
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          if [ -n "$TELEGRAM_TOKEN" ] && [ -f trendwave_log.txt ]; then
            PREFIX=$'🌟 نتائج trendwave (${{ inputs.timeframe }} · hl_gate=${{ inputs.hl_gate }} · w1_targets=${{ inputs.w1_targets }} · ${{ inputs.extra_gates }})\n\n'
            BODY=$(tail -c 3500 trendwave_log.txt)
            curl -s "https://api.telegram.org/bot${TELEGRAM_TOKEN}/sendMessage" \
              --data-urlencode "chat_id=${TELEGRAM_CHAT_ID}" \
              --data-urlencode "text=${PREFIX}${BODY}" \
              --data "disable_web_page_preview=true" >/dev/null || true
          fi

      - name: Upload CSV artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: trendwave-results
          path: |
            backtest_*.csv
            trendwave_log.txt
          if-no-files-found: ignore
