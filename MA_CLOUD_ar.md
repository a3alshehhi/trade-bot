# بوت سحابة المتوسطات (MA Cloud) 📊

## نظرة عامة

بوت تحليلي **مستقل تماماً** لاستراتيجية **سحابة المتوسطات** — يجمع 5 متوسطات متحركة معاً لتحديد نقاط دخول احترافية:

- **EMA20 High/Low** → سحابة سريعة (زخم قصير)
- **EMA100 High/Low** → سحابة بطيئة (اتجاه متوسط)
- **SMA300** → فلتر النظام (bull/bear regime)

---

## الاستراتيجية

### شروط الدخول

#### 🟢 شراء (LONG)
1. **فلتر النظام**: الإغلاق **فوق** SMA300
2. **تأكيد الاتجاه**: سحابة EMA100 **بالكامل** فوق SMA300 (ليس مجرد لمسة)
3. **الزناد**: 
   - السعر يلمس أو يدخل سحابة EMA20 (تصحيح)
   - ثم يغلق **فوق** EMA20 High مجدداً (استعادة الاختراق)
   - البُعد من EMA20 عند الإشارة ≤ 5% (تجنب المطاردة)

#### 🔴 بيع (SHORT)
نفس الشروط لكن معكوسة:
- الإغلاق **تحت** SMA300
- سحابة EMA100 **بالكامل** تحت SMA300
- السعر يلمس EMA20 ثم يغلق **تحت** EMA20 Low

---

## نقاط الخروج

### وقف الخسارة
- تحت آخر **قاع تصحيح** أو تحت **EMA100 Low** (أيهما أقرب أماناً)
- شرط إجباري: انكسار هيكلي لـ EMA100 = خروج فوري

### الأهداف (فيبوناتشي امتداد)
- **الهدف 1** (TP1): Entry + 1.272 × طول الموجة الدافعة
- **الهدف 2** (TP2): Entry + 1.618 × طول الموجة الدافعة

### إدارة المركز (50/50)
1. عند الهدف الأول: **جني 50% الرصيد** + **نقل الوقف للتعادل**
2. عند الهدف الثاني: **جني 50% الباقية** + **إغلاق كامل**

---

## الاستخدام

### الباك-تست المحلي
```bash
python ma_cloud_bot.py backtest
```

أو مع إعدادات مخصصة:
```bash
MC_BT_FRAMES="15m,1h" MC_BT_SYMBOLS=30 MC_BT_HOLD=100 python ma_cloud_bot.py backtest
```

**المتغيرات البيئية:**
- `MC_BT_FRAMES` — فريمات الباك-تست (افتراضي: `15m,1h,4h`)
- `MC_BT_SYMBOLS` — عدد الرموز المختبرة (افتراضي: 40)
- `MC_BT_HOLD` — أقصى شموع لإمساك الصفقة (افتراضي: 200)
- `MC_TF` — فريم الـ scan الحي (افتراضي: `1h`)

### الـ Scan الحي
```bash
python ma_cloud_bot.py scan
```

أو اختصار الأمر:
```bash
python ma_cloud_bot.py
```

سيبحث عن أفضل الإشارات ويرسلها لتيليجرام (إن تم ضبط `TELEGRAM_TOKEN` و `TELEGRAM_CHAT_ID`).

---

## الباك-تست على GitHub Actions

### التشغيل اليدوي
1. انتقل إلى **GitHub** → المستودع
2. **Actions** → **MA Cloud Backtest**
3. **Run workflow** (أعلى اليمين)
4. اختر الفريمات والرموز والـ hold (أو اتركها افتراضية)
5. اضغط **Run workflow**

### النتائج
- تُحفظ في artifact باسم `ma-cloud-backtest-result`
- نتيجة شاملة لكل فريم:
  - عدد الإشارات المكتشفة
  - نسبة الفوز (Win Rate)
  - العائد المتوقع لكل صفقة (Expectancy)
  - عامل الربح (Profit Factor)

---

## المتطلبات

- **Python 3.7+**
- **requests** (جلب البيانات من Binance Vision)
- ملف `watchlist.txt` في نفس المجلد

### Watchlist Format
```
BINANCE:BTCUSDT, BINANCE:ETHUSDT, BINANCE:BNBUSDT
BYBIT:SOLUSDT, BYBIT:ADAUSDT
```

أو نسخ مباشر من TradingView.

---

## الحالة والذاكرة

- ملف `ma_cloud_state.json` يتتبع الإشارات المرسلة (منع التكرار)
- يُحفظ تلقائياً بعد كل scan
- يمكن مسحه يدوياً لإعادة تشغيل الـ scan

```bash
rm ma_cloud_state.json
```

---

## الاختلافات عن البوتات الأخرى

| الميزة | SD Bot | Hunter | MA Cloud |
|--------|--------|--------|----------|
| الاستراتيجية | عرض/طلب + ML | صيد ارتفاعات | سحابة متوسطات |
| الفريمات | 15m + 1h | 15m + أعلى | 15m + 1h + 4h |
| الدفتر | `sd_ledger.json` | `hunter_ledger.json` | `ma_cloud_state.json` |
| التنفيذ الحي | Bybit/Binance Demo | Bybit/Binance Demo | **تحليل فقط** |

---

## ملاحظات مهمة

⚠️ **هذا البوت أداة تحليلية تعليمية — لا ينفّذ صفقات بأموال حقيقية**

- النتائج تعتمد على البيانات التاريخية
- التداول ينطوي على مخاطر كبيرة
- استخدم الباك-تست للتحقق من الاستراتيجية قبل أي قرار تداول
- ليست نصيحة مالية

---

## المساعدة والمشاكل

### لا توجد إشارات في الـ scan
- تحقق من `watchlist.txt`
- اتأكد أن البيانات تُحمّل من Binance بنجاح
- قد تحتاج الأسواق لوقت أطول لتشكيل الإشارات

### الباك-تست يعطي نتائج غريبة
- جرّب زيادة `MC_BT_HOLD` (أكثر من 200)
- غيّر `MC_BT_FRAMES` لفريم واحد
- تحقق من حجم البيانات المجلوبة

### مشكلة تيليجرام
- تأكد من `TELEGRAM_TOKEN` و `TELEGRAM_CHAT_ID` صحيحة
- استخدم `@BotFather` على تيليجرام لإنشاء بوت جديد

---

## التطوير المستقبلي

- [ ] إضافة فلاتر ML للتصفية
- [ ] تتبع صفقات مباشر على تيليجرام
- [ ] Dashboard تفاعلي للنتائج
- [ ] دعم منصات إضافية (Bybit، MEXC)

---

**آخر تحديث:** 2026-07-12
**الحالة:** جاهز للباك-تست والـ scan الحي
