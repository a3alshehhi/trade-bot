# 🚀 إعداد وتشغيل بوت سحابة المتوسطات

## ✅ ما تم إعداده

تم بناء **بوت مستقل تماماً** لاستراتيجية سحابة المتوسطات (EMA20/EMA100/SMA300):

### الملفات الأساسية
- ✅ `ma_cloud_bot.py` — البوت الرئيسي (جاهز)
- ✅ `ma_cloud_run.sh` — سكريبت تشغيل سريع (جاهز)
- ✅ `.github/workflows/ma_cloud_backtest.yml` — وركفلو باك-تست (جاهز)
- ✅ `MA_CLOUD_ar.md` — التوثيق الكامل

### الفريمات المدعومة
- ✅ **15 دقيقة** (15m)
- ✅ **ساعة واحدة** (1h)
- ✅ **4 ساعات** (4h)

---

## 🎯 التشغيل السريع

### الخيار 1: الـ Scan الحي (البحث عن الإشارات الآن)

#### على ماك/لينكس
```bash
cd ~/Claude/Projects/"AI Trading bot"
./ma_cloud_run.sh scan
```

أو مباشرة:
```bash
python3 ma_cloud_bot.py scan
```

#### النتيجة
- 🟢 **الإشارات الجديدة** تُطبع في الـ console
- 📱 **إرسال تيليجرام** (لو ضبطت التوكن والـ chat ID)

---

### الخيار 2: الباك-تست على 3 فريمات (محلي)

#### تشغيل سريع (الإعدادات الافتراضية)
```bash
./ma_cloud_run.sh backtest
```

#### تشغيل مخصص
```bash
# فريمات مختلفة
MC_BT_FRAMES="15m,1h" ./ma_cloud_run.sh backtest

# عدد رموز مختلف
MC_BT_SYMBOLS=30 ./ma_cloud_run.sh backtest

# hold مختلف
MC_BT_HOLD=100 ./ma_cloud_run.sh backtest

# الثلاثة معاً
MC_BT_FRAMES="15m,4h" MC_BT_SYMBOLS=50 MC_BT_HOLD=150 ./ma_cloud_run.sh backtest
```

#### النتيجة
تقرير شامل لكل فريم:

```
════ فريم 15m ════
رموز مُحلَّلة=40
  LONG  : إشارات=145 · صفقات=145 · فوز=62.1% · توقّع=+0.456R · PF=1.87
  SHORT : إشارات=132 · صفقات=132 · فوز=58.3% · توقّع=+0.234R · PF=1.43
  الكل  : صفقات=277 · فوز=60.3% · توقّع=+0.351R · PF=1.65

════ فريم 1h ════
...
```

---

### الخيار 3: الباك-تست على GitHub Actions (سحابي)

#### الخطوات
1. **اذهب لـ GitHub** → المستودع الخاص بك
2. **الصفحة:** Actions
3. **الـ Workflow:** اختر **"MA Cloud Backtest"**
4. **اضغط:** "Run workflow" (أعلى اليمين)
5. **الخيارات** (اختيارية):
   - Frames: `15m,1h,4h` (افتراضي)
   - Symbols: `40` (افتراضي)
   - Hold: `200` (افتراضي)
6. **اضغط:** "Run workflow"

#### الانتظار والنتائج
- ⏳ الانتظار ~2-3 دقائق (حسب عدد الرموز)
- 📥 **Download** artifact باسم `ma-cloud-backtest-result`
- 📊 الملف يحتوي النتائج الكاملة

---

## 📊 فهم النتائج

### مثال على نتيجة
```
صفقات=277 · فوز=60.3% · توقّع=+0.351R · PF=1.65 · مجموع=+97.3R
```

| المقياس | المعنى |
|--------|--------|
| `صفقات=277` | عدد الصفقات المختبرة |
| `فوز=60.3%` | نسبة الصفقات الرابحة |
| `توقّع=+0.351R` | متوسط العائد لكل صفقة (R = خطر الوقف) |
| `PF=1.65` | عامل الربح (إجمالي الأرباح ÷ إجمالي الخسائر) |
| `مجموع=+97.3R` | الربح الكلي (بوحدات R) |

### تقييم الأداء
- ✅ **جيد جداً:** PF > 1.5، توقّع > +0.3R
- ✅ **جيد:** PF > 1.3، توقّع > +0.1R
- ⚠️ **متوسط:** PF > 1.0، توقّع > 0R (رابح بالكاد)
- ❌ **ضعيف:** PF < 1.0 أو توقّع < 0R (خاسر)

---

## 🔧 الإعدادات المتقدمة

### متغيرات البيئة

#### للباك-تست
```bash
MC_BT_FRAMES    # فريمات الباك-تست (افتراضي: 15m,1h,4h)
MC_BT_SYMBOLS   # عدد الرموز (افتراضي: 40)
MC_BT_HOLD      # أقصى شموع لإمساك الصفقة (افتراضي: 200)
```

#### للـ Scan الحي
```bash
MC_TF           # فريم الـ scan (افتراضي: 1h)
MC_STATE        # مسار حفظ الحالة (افتراضي: ma_cloud_state.json)
TELEGRAM_TOKEN  # توكن البوت (من @BotFather)
TELEGRAM_CHAT_ID# معرّف المحادثة
```

### مثال: تشغيل scan بفريم 15 دقيقة
```bash
MC_TF="15m" TELEGRAM_TOKEN="123..." TELEGRAM_CHAT_ID="456..." python3 ma_cloud_bot.py scan
```

---

## 🐛 حل المشاكل

### المشكلة: "خطأ في جلب البيانات"
**الحل:**
```bash
# تحقق من الاتصال
curl -s https://data-api.binance.vision/api/v3/ping

# إذا فشل، جرّب API الآخر
# البوت يتبدل تلقائياً بين:
# - https://data-api.binance.vision
# - https://api.binance.com
```

### المشكلة: "لا توجد إشارات"
**الأسباب والحلول:**
1. البيانات ناقصة: جرّب زيادة `MC_BT_SYMBOLS`
2. الفريم غير مناسب: جرّب `15m` بدلاً من `4h`
3. السوق هادئ: الاستراتيجية تحتاج حركة كافية

### المشكلة: "البوت بطيء"
**الحل:**
- قلل عدد الرموز: `MC_BT_SYMBOLS=20`
- قلل الفريمات: `MC_BT_FRAMES="15m,1h"` (بدون 4h)
- قلل الـ hold: `MC_BT_HOLD=100`

### المشكلة: "تيليجرام لا يعمل"
**الحل:**
```bash
# 1. أنشئ بوت جديد في @BotFather
# 2. احصل على التوكن والـ chat ID
# 3. اختبره:
curl -X POST https://api.telegram.org/botTOKEN/sendMessage \
  -d chat_id=CHATID -d text="اختبار"
```

---

## 📋 Watchlist

تأكد من وجود ملف `watchlist.txt` في نفس المجلد:

### مثال
```
BINANCE:BTCUSDT,BINANCE:ETHUSDT,BINANCE:BNBUSDT,BINANCE:SOLUSDT
BYBIT:ADAUSDT,BYBIT:DOGEUSDT
MEXC:XRPUSDT
```

أو نسخ مباشر من TradingView (سيُقرأ تلقائياً).

---

## 🔄 الجدولة التلقائية

### خيار 1: GitHub Actions (مجاني 24/7)

اتبع `CLOUD_SETUP_ar.md` لتفعيل جدولة تلقائية على GitHub.

### خيار 2: Cron Job محلي

```bash
# اختبر البوت أولاً
crontab -e

# أضف سطر (كل 15 دقيقة)
*/15 * * * * cd ~/Claude/Projects/"AI Trading bot" && ./ma_cloud_run.sh scan >> ma_cloud.log 2>&1

# أو كل ساعة
0 * * * * cd ~/Claude/Projects/"AI Trading bot" && ./ma_cloud_run.sh scan >> ma_cloud.log 2>&1
```

---

## ⚠️ تنبيهات مهمة

1. **أداة تحليلية فقط** — لا تنفّذ صفقات حقيقية
2. **لا تشارك الحالة** مع البوتات الأخرى (sd_bot, hunter_bot)
3. **التداول مخاطر** — النتائج الماضية لا تضمن المستقبل
4. **لا نصيحة مالية** — أنت المسؤول عن قراراتك

---

## ✨ الخطوة التالية

1. **جرّب الـ scan أولاً:** `./ma_cloud_run.sh scan`
2. **شغّل باك-تست:** `./ma_cloud_run.sh backtest`
3. **إذا أعجبتك النتائج:** فكّر في الجدولة التلقائية
4. **اقرأ التوثيق الكامل:** `MA_CLOUD_ar.md`

---

**آخر تحديث:** 2026-07-12  
**الحالة:** 🟢 جاهز للاستخدام الفوري
